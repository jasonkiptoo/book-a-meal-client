import React from 'react'
// import MenuCard from './MenuCard';

const RenderOrder = ({ meal}) => {
  return (
    <div>
    {/* {meal.map((item) => (
        // console.log(item.name)

      <div key={item.id} className="meals-item">
        <p>{item.name}</p>
      </div>
    ))} */}
  </div>
  );
};

export default RenderOrder
