import React from "react";



const OrderSummary=({cartItems})=>{

return(<div>
   { console.log({cartItems})}
{/* {cartItems.map((items)=>{<h1>{items}</h1> }) } */}
</div>)
}
export default OrderSummary
